LightTerm 项目文档
项目概述
LightTerm是一款为Android平台打造的极致轻量、高性能SSH客户端。其核心使命是在保证极低资源占用的前提下，提供稳定、流畅的远程命令行体验。本次迭代的重点是支持多终端并发连接与会话管理，允许用户同时维护多个SSH会话，并能在它们之间实现毫秒级无缝切换，满足专业开发者在移动场景下的复杂运维需求。
核心功能需求
1. SSH连接管理
● 支持SSHv2协议，兼容主流SSH服务器（如OpenSSH）
● 支持密码认证与公钥认证（RSA/ECDSA/ED25519），私钥通过Android Keystore加密存储
● 支持保存并管理多个服务器配置（别名、主机、端口、用户名、认证方式）
● 具备后台保活与智能断线重连机制（可配置重试次数与间隔）
2. 多终端会话管理（新增核心）
● 多标签页支持：用户可同时建立并维持多个独立的SSH连接，每个连接在一个标签页中运行
● 流畅切换：通过滑动或点击标签，实现不同终端会话间的快速切换，切换过程无卡顿、无白屏，目标延迟低于200ms
● 会话状态保持：每个终端会话拥有独立的缓冲区、窗口尺寸和运行状态。切换回某一标签时，应精确恢复其上次离开时的显示内容和上下文
● 会话标识：每个标签页需清晰显示服务器别名或自定义标题，便于用户识别
3. 终端显示优化
● 采用高效终端渲染引擎（如JackPal Android Terminal Emulator），支持ANSI 256色及真彩色
● 支持文本缩放、等宽字体选择与高亮显示
● 实现增量渲染与环形缓冲区（Ring Buffer），限制单会话最大行数（如1000行），防止内存溢出
● 支持长文本自动换行与平滑滚动，确保高帧率渲染
4. 性能与资源优化
● 内存控制：通过环形缓冲区限制单会话内存占用，整体应用内存峰值控制在200MB以内（中端设备）
● CPU优化：采用异步非阻塞I/O模型处理网络I/O，UI刷新采用增量更新策略
● 电池友好：后台会话自动降低心跳频率，提供"省电模式"（关闭颜色与动画）
5. 基础交互功能
● 可折叠虚拟键盘，支持Ctrl、Alt、Tab、Esc等常用快捷键
● 长按复制/粘贴，无缝对接系统剪贴板
● 本地命令历史记录，支持上下键快速调用
● （可选）基础SFTP文件传输功能
技术实现要点
1. 会话管理架构
● 采用SessionManager单例模式全局管理所有SSH会话
● 每个SSH会话（SSHSession）封装独立的SocketChannel、输入/输出流、终端缓冲区及认证状态
● 使用ViewModel + StateFlow（Kotlin协程）实现UI层与会话状态的响应式同步
2. 并发与线程模型
● 每个SSH会话运行在独立的后台线程或协程中，避免相互阻塞
● 利用Kotlin的Dispatchers.IO处理网络I/O，Dispatchers.Main处理UI更新
● 通过CoroutineScope管理会话生命周期，确保切换或关闭时资源正确释放
3. UI与渲染优化
● 终端视图采用RecyclerView或自定义SurfaceView实现，支持高效复用与局部刷新
● 标签页切换使用ViewPager2，启用offscreenPageLimit预加载相邻页面，提升切换流畅度
● 颜色方案与字体配置通过SharedPreferences持久化，启动时快速加载
4. 数据存储
● 服务器配置与会话元数据使用Room数据库存储
● 敏感信息（如私钥、密码）通过Android Keystore加密后存入EncryptedSharedPreferences
性能指标（目标）
指标类别
目标值
备注
启动时间
冷启动 < 1秒


内存占用
空闲时 < 50MB，每增加一个活跃会话 < +30MB
中端设备
切换延迟
标签页切换动画帧率 ≥ 60fps，内容渲染延迟 < 200ms


CPU占用
单会话无操作时 < 1%，多会话（3个）活跃时 < 8%
中端设备
流畅度
终端滚动与输出渲染帧率 ≥ 50fps




用户界面设计原则
1. 极简高效
● 主界面为标签栏+终端区域，无冗余元素
● 仅保留核心功能，避免功能臃肿
2. 深色主题默认
● 降低OLED屏幕功耗，保护视力
● 符合开发者使用习惯
3. 单手友好
● 标签栏支持滑动操作
● 虚拟键盘可左右浮动，适应不同握持姿势
4. 状态可见
● 连接状态（已连接/断开/重连中）通过标签颜色或图标清晰指示
● 提供直观的用户反馈
开发优先级（MVP v2.0）
第一阶段：基础功能
1. 实现单会话SSH连接与终端渲染（基础功能）
2. 开发SessionManager与多标签页UI框架
第二阶段：核心功能
3. 实现会话独立线程模型与状态隔离
4. 集成ViewPager2并优化切换流畅度
第三阶段：完善与优化
5. 完善会话状态保存与恢复机制
6. 性能压测与内存/CPU优化
技术栈推荐
语言与框架
● 编程语言：Kotlin（100% 原生，避免Java互操作开销）
● 架构模式：MVVM（ViewModel + LiveData/StateFlow）
● 异步处理：Kotlin协程（Coroutine）
核心库与组件
● SSH库：Apache MINA sshd（纯Kotlin/Java实现，轻量级）或 JSch（成熟稳定）
● 终端渲染：JackPal Android Terminal Emulator（开源，已优化Android兼容性）
● 数据存储：Room数据库 + EncryptedSharedPreferences
● UI组件：ViewPager2 + RecyclerView
开发工具
● IDE：Android Studio
● 版本控制：Git
● 构建系统：Gradle
兼容性要求
Android版本
● 最低支持：Android 8.0（API 26）
● 目标版本：Android 14（API 34）
● 兼容设备：覆盖主流中低端到高端设备
权限要求
● 必需权限：
○ 网络访问权限（INTERNET）
○ 存储权限（READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE - 仅用于SFTP文件传输）
● 权限原则：最小权限原则，不请求任何非必要权限
安全性设计
数据安全
● 传输安全：SSH协议本身提供加密传输
● 存储安全：敏感信息通过Android Keystore加密存储
● 内存安全：及时清理敏感数据，避免内存泄露
认证安全
● 密钥管理：私钥不以明文形式存储
● 认证流程：支持公钥认证，避免密码明文传输
项目愿景
LightTerm致力于成为Android平台上最轻量、最流畅的多会话SSH客户端。通过极致的性能优化与简洁的交互设计，让开发者在移动设备上也能获得媲美桌面的高效运维体验，真正做到"口袋里的命令中心"。
后续扩展计划
短期规划（v2.1）
● 完善SFTP文件传输功能
● 增加会话分组管理
● 支持自定义快捷命令
中期规划（v3.0）
● 实现终端分屏功能
● 增加命令自动补全
● 支持主题自定义
长期愿景
● 跨平台支持（iOS、Web）
● 插件扩展机制
● 云同步配置



文档版本：v2.0
最后更新：2024年
